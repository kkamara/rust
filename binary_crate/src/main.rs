// Example binary crate setup.
// Contains a main.rs that serves
// as the entry point for the binary.
// Can also include a lib.rs if it
// wants to expose a library API, but
// it's not required.
fn main() {
    println!("Hello, world!");
}
// See project library_crate.
// See project backyard.